# Backend API base URL — must include /api suffix
# Set this in the Railway dashboard for the web service BEFORE deploying.
# Use your Railway backend service URL (from step 6 in DEPLOYMENT.md).
VITE_API_URL=https://your-backend.up.railway.app/api
